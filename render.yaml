services:
  - type: web
    name: aluminium-imputation-api
    env: python
    buildCommand: pip install -r requirements.txt
    startCommand: uvicorn aluminium_input_api:app --host 0.0.0.0 --port 10000
    plan: free
